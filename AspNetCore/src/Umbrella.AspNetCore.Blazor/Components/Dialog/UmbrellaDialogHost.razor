@namespace Umbrella.AspNetCore.Blazor.Components.Dialog

@foreach (var entry in DialogService.ActiveDialogs)
{
    <CascadingValue @key="entry.Instance" Value="@entry.Instance">
        <DynamicComponent Type="@entry.ComponentType" Parameters="@entry.Parameters.ToDictionary()" />
    </CascadingValue>
}
