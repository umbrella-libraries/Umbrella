@namespace Umbrella.AspNetCore.Blazor.Components.Dialog

@foreach (var entry in DialogService.ActiveDialogs)
{
    <CascadingValue Value="@entry.Instance">
        <DynamicComponent @key="entry.Instance" Type="@entry.ComponentType" Parameters="@entry.Parameters.ToDictionary()" />
    </CascadingValue>
}
