<div class="u-responsive-image @CssClass">
    <img src="@Url" srcset="@(string.IsNullOrWhiteSpace(SrcSetValue) ? null : SrcSetValue)" loading="@(LazyLoadingEnabled ? "lazy" : null)" decoding="@(LazyLoadingEnabled ? "async" : null)" @attributes="AdditionalImageAttributes" />
</div>
